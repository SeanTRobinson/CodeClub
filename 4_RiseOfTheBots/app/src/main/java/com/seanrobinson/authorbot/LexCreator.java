package com.seanrobinson.authorbot;

public class LexCreator {

    public LexCreator() {

    }

    public void createLexicon(String inputPath, String outputPath) {
        /*
        open file
        add each word in file to list
        convert list to set
        for each word in set: (1)
          for each word in list: (2)
            if 2 == 1 and 2[-1] is valid word:
              add 2[+1] to new list
            1.followers = new list
        write json to file
         */
    }
}
